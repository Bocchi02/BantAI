Read the repository instructions before editing.

Make the requested popup UI change while preserving:
- Roboto font;
- simple digital-literacy language;
- SAFE and SUSPICIOUS terminology;
- technical scores under More details;
- five-second automatic popup only for newly opened supported emails;
- manual popup behavior;
- current detector scopes.

Do not change backend models, thresholds, permissions, or provider extractors.

Run `python scripts\verify_project.py` and summarize the diff.
