Read the repository instructions first.

Run the non-model baseline checks:

```powershell
python scripts\verify_project.py
```

Do not load, download, retrain, or replace any model.

Report:
- commands executed;
- files inspected;
- checks passed;
- checks failed;
- recommended next action.

Do not edit files unless a failed check has an obvious, minimal source-only fix.
